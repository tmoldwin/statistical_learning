__pycache__/
*.pyc
*.log
.venv/
venv/
.idea/
.cursor/

# Generated experiment artifacts (run_experiments.py / visualize.py)
# .npz model files are stored via Git LFS (see .gitattributes). They contain
# only final weights + training curves (~MBs). Don't commit models trained
# with --save-snapshots (weight-snapshot history, can be GBs); run
# scripts/strip_npz_snapshots.py first.
experiments/**/model.pt
experiments/**/plots/
experiments/**/input.txt
experiments/**/shared/

# Local download / scratch artifacts
*.tar.gz
h500_checkpoints.tar.gz

